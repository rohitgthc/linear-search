# I am writing a linear search program.

def Linear_Search(p, x):
    i = 0
    while i < len(p):
        if p[i] == x:
            print('position of', x , 'is' ,  i)
            exit()
        i = i + 1
    print ('posiotin of', x , 'is not available')


a = [8, 2, 6, 5, 3, 9, 1, 0]  # This is the LIST
b = 6  # We have to find the position of x
c = Linear_Search(a, b)
